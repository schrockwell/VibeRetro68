#ifndef MYAPP_ABOUT_H
#define MYAPP_ABOUT_H

/* Show the About window (WIND 129). Modal: dismissed on any click or
   keystroke. Returns when the window has been disposed. */
void DoAbout(void);

#endif
